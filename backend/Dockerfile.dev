FROM node:20-alpine

# Install required system packages for testing
RUN apk add --no-cache sqlite curl

WORKDIR /app

COPY package*.json ./
COPY prisma ./prisma/
RUN npm install --legacy-peer-deps
RUN npx prisma generate

EXPOSE 3101

CMD ["sh", "-c", "npx prisma generate && npm run dev"]