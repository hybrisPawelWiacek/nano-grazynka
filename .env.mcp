# nano-grazynka MCP Configuration
# Generated: 2025-01-11
# Purpose: Project-specific MCP server configurations and policies

# === BLOCKED SERVERS ===
# Use native tools instead of claude-code MCP wrapper
export DISABLE_CLAUDE_CODE_MCP="true"

# === SERENA CONFIG ===
# Semantic code analysis for this project
export SERENA_PROJECT_PATH="${PWD}"
export SERENA_LANGUAGE="typescript"

# === MEMORY CONFIG ===
# Persistent storage and context management
export MEMORY_STORAGE_PATH="${PWD}/.memory"
export MEMORY_AUTO_SAVE="true"
export MEMORY_CONTEXT_THRESHOLD="94"
export MEMORY_CHECKPOINT_PREFIX="CONTEXT_CHECKPOINT"

# === GITHUB CONFIG ===
# Version control and research repositories
export GITHUB_DEFAULT_REPO="hybrisPawelWiacek/nano-grazynka"
export GITHUB_RESEARCH_REPOS="openai/whisper,ggerganov/whisper.cpp,xenova/whisper-web"
export GITHUB_USE_CLI_FOR_LOCAL="true"
export GITHUB_USE_MCP_FOR_REMOTE="true"

# === PLAYWRIGHT CONFIG ===
# E2E testing configuration
export PLAYWRIGHT_BASE_URL="http://localhost:3100"
export PLAYWRIGHT_BACKEND_URL="http://localhost:3101"
export PLAYWRIGHT_TIMEOUT="30000"
export PLAYWRIGHT_HEADED="false"
export PLAYWRIGHT_SLOWMO="0"

# === FIRECRAWL CONFIG ===
# Web scraping settings
export FIRECRAWL_MAX_DEPTH="2"
export FIRECRAWL_CACHE_TTL="3600"
export FIRECRAWL_PREFER_MARKDOWN="true"

# === PERPLEXITY CONTEXT ===
# Research context for better targeted searches
export PERPLEXITY_PROJECT_CONTEXT="nano-grazynka: voice transcription MVP with Whisper API"
export PERPLEXITY_TECH_STACK="Next.js 15, Fastify, TypeScript, Prisma, SQLite"
export PERPLEXITY_FOCUS="MVP solutions, audio processing, transcription accuracy"

# === PROJECT SERVICES ===
# Local development URLs
export FRONTEND_URL="http://localhost:3100"
export BACKEND_URL="http://localhost:3101"
export DATABASE_PATH="${PWD}/data/nano-grazynka.db"

# === AUDIO PROCESSING ===
# Audio file handling configuration
export MAX_AUDIO_FILE_SIZE="100MB"
export SUPPORTED_AUDIO_FORMATS="mp3,m4a,wav,webm"
export WHISPER_MODEL="whisper-1"
export TRANSCRIPTION_LANGUAGES="en,pl"